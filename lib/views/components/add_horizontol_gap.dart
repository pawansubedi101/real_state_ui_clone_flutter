import 'package:flutter/material.dart';

class AddHorizontolGap extends StatelessWidget {
  const AddHorizontolGap({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 10,
    );
  }
}
